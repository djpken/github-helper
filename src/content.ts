// Content script — injected into github.com pages.
// ponytail: matches github.com/* as a sensible default for a "github-helper"; narrow/change once the purpose is set.
console.log('[github-helper] content script loaded')
